jest.dontMock('react');

describe('containers/EditStyleDialog', () => {
    const connect = require('react-redux').connect;

    jest.dontMock('../EditStyleDialog');
    const EditStyleDialog = require('../EditStyleDialog');
});